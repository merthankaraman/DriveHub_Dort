<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.drivehub.dort"
    android:sharedUserId="android.uid.system">

    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.WRITE_SECURE_SETTINGS"
        tools:ignore="ProtectedPermissions" />
    <uses-permission android:name="android.permission.MEDIA_CONTENT_CONTROL"
        tools:ignore="ProtectedPermissions" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation" />
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.car.permission.CAR_ENERGY" />
    <uses-permission android:name="android.car.permission.CAR_POWERTRAIN" />
    <uses-permission android:name="android.car.permission.CAR_VENDOR_EXTENSION"
        tools:ignore="ProtectedPermissions" />
    <!-- CarDiagnosticManager — canlı OBD/teşhis çerçevesi (sistem uygulaması) -->
    <uses-permission android:name="android.car.permission.CAR_DIAGNOSTICS"
        tools:ignore="ProtectedPermissions" />
    <uses-permission android:name="android.car.permission.CONTROL_CAR_CLIMATE"
        tools:ignore="ProtectedPermissions" />

    <uses-feature android:name="android.hardware.type.automotive" android:required="false" />

    <application
        android:allowBackup="false"
        android:persistent="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:theme="@style/Theme.DriveHubDort"
        android:localeConfig="@xml/locale_config">

        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:screenOrientation="landscape"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.DrivingHistoryActivity"
            android:exported="false" />

        <activity
            android:name=".ui.ChargingHistoryActivity"
            android:exported="false"
            android:screenOrientation="landscape"
            android:theme="@style/Theme.DriveHubDort" />

        <activity
            android:name=".ui.DemoActivity"
            android:exported="false"
            android:screenOrientation="landscape"
            android:theme="@style/Theme.DriveHubDort" />

        <!-- Gömülü kadran (eski com.drivehub.kadran APK) -->
        <activity
            android:name="com.drivehub.kadran.DashboardActivity"
            android:exported="false"
            android:label="@string/btn_open_gauge_app"
            android:configChanges="uiMode|orientation|screenSize|screenLayout|smallestScreenSize|keyboardHidden"
            android:screenOrientation="sensorLandscape"
            android:theme="@style/Theme.DriveHub_Kadran" />

        <!-- Dış uygulamalardan motor sesini AÇMA Activity'si -->
        <activity
            android:name=".ui.StartEngineSoundActivity"
            android:exported="true"
            android:excludeFromRecents="true"
            android:theme="@style/Theme.DriveHubDort">
            <intent-filter>
                <action android:name="com.drivehub.dort.action.START_ENGINE_SOUND_ACTIVITY" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <!-- Dış uygulamalardan motor sesini KAPATMA Activity'si -->
        <activity
            android:name=".ui.StopEngineSoundActivity"
            android:exported="true"
            android:excludeFromRecents="true"
            android:theme="@style/Theme.DriveHubDort">
            <intent-filter>
                <action android:name="com.drivehub.dort.action.STOP_ENGINE_SOUND_ACTIVITY" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <service
            android:name=".service.MG4ControlService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="vehicle_control" />
        </service>

        <receiver
            android:name=".receiver.BootReceiver"
            android:exported="true">
            <intent-filter android:priority="1000">
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>

        <!-- Telemetri: gömülü kadran ve dış istemciler ContentProvider ile okur -->
        <provider
            android:name=".telemetry.TelemetryProvider"
            android:authorities="com.drivehub.dort.telemetry"
            android:exported="true"
            android:directBootAware="false" />

    </application>
</manifest>