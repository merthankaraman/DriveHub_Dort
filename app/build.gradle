plugins {
    alias(libs.plugins.android.application)
}

android {
    namespace 'com.example.mg4_v3'
    compileSdk {
        version = release(36) {
            minorApiLevel = 1
        }
    }

    defaultConfig {
        applicationId "com.example.mg4_v3"
        minSdk 28
        targetSdk 36
        versionCode 1
        versionName "1.4.2.6"

        testInstrumentationRunner "androidx.test.runner.AndroidJUnitRunner"
    }

    flavorDimensions "env"

    productFlavors {
        car {
            dimension "env"
            // Araç için orijinal paket adı
            applicationId "com.example.mg4_v3"
        }
        sim {
            dimension "env"
            // Simülasyon / emulator için ayrı paket
            applicationId "com.example.mg4_v3.sim"
        }
    }

    buildTypes {
        debug {
            // Android Studio Run butonu → debug keystore (hızlı geliştirme)
            // Araca yüklemek için sign_and_install.bat kullan
        }
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
        }
    }
    compileOptions {
        sourceCompatibility JavaVersion.VERSION_11
        targetCompatibility JavaVersion.VERSION_11
    }
}

dependencies {
    implementation libs.appcompat
    implementation libs.material
    implementation libs.activity
    implementation libs.constraintlayout
    testImplementation libs.junit
    androidTestImplementation libs.ext.junit
    androidTestImplementation libs.espresso.core
}